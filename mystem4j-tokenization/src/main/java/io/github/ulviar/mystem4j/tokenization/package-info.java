/**
 * Search-token preparation above parsed MyStem model objects.
 *
 * <p>The package keeps offset-safety behavior always on and exposes optional semantic enrichment through
 * {@link io.github.ulviar.mystem4j.tokenization.MystemSearchTokenizerOptions}.
 */
package io.github.ulviar.mystem4j.tokenization;
